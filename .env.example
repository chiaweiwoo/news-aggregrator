# Supabase → Project Settings → API → Project URL (no trailing slash)
SUPABASE_URL=

# Supabase → Project Settings → API → service_role key (backend only, keep secret)
SUPABASE_SERVICE_KEY=

# Google AI Studio → aistudio.google.com → Get API Key
GEMINI_API_KEY=
